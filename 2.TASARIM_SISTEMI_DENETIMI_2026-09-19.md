# Apartora — Tasarım Sistemi ve UX Denetimi

> **Tarih:** 2026-09-19 · **HEAD:** `514f2a5c5` · **Kapsam:** web (`src/`) + mobil (`apps/mobile/src`) + `packages/shared`
> **Nitelik:** Salt okuma denetimi. Kod değiştirilmedi. Sayılar betikle ölçüldü (testler, story'ler, `node_modules` hariç; yorum satırları ayıklandı).
> **İlişkili belge:** Aynı gün yapılan **mobil yönetici** denetimi (`apps/mobile/docs/ux-denetim/RAPOR.md`, `DEVAM_RAPORU.md`, `sonuc/tema.json`, `sonuc/yapi.md`). O denetimin bulguları burada tekrar edilmedi, yalnız atıf yapıldı. Bu belge onun kapsamadığı üç alanı ekler: **web**, **yönetici dışındaki roller** ve **tasarım sistemi (token/bileşen) katmanı**.

## 0. Uygulama bilgileri (şablondaki boşluklar koddan dolduruldu)

| Alan | Değer | Kaynak |
|---|---|---|
| Uygulama adı | Apartora (Apartora Manager) | `CLAUDE.md` |
| Platform | İkisi: web (apartora.com, masaüstü + mobil tarayıcı/PWA) ve mobil uygulama (Android + iOS) | `CLAUDE.md` ÜÇ HEDEF kuralı |
| Teknoloji | Web: Next.js 16 + React 19 + Tailwind + shadcn/ui · Mobil: Expo (React Native) + NativeWind + react-native-reusables · Ortak: `@apartora/shared` | `package.json`, `tailwind.config.ts`, `apps/mobile/tailwind.config.js` |
| Kullanıcı rolleri | superadmin, semiadmin, **manager (yönetici)**, semimanager (yardımcı yönetici), **security (güvenlik görevlisi)**, **cleaning / staff (temizlik / görevli)**, **resident (sakin: malik/kiracı)**, **external (dış görevli: muhasebeci, avukat, denetçi)** | `packages/shared/src/roles.ts:22-70` |
| Mevcut renkler | Birincil mavi `#2563EB` (koyu: `#3B82F6`), zemin `#F8FAFC` (koyu: `#020817`), kart `#FFFFFF` (koyu: `#0F1729`). Tam tablo §1.1 | `src/styles/globals.css:262-500`, `apps/mobile/src/global.css` |
| Mevcut font | Web: Plus Jakarta Sans (gövde, 4 ağırlık) + DM Sans (başlık, 3 ağırlık) · Mobil: sistem fontu (SF Pro / Roboto) | `src/app/layout.tsx:25-40`, `apps/mobile/src/constants/theme.ts:48-72` |
| Varsayılan tema | Web: **açık** (`ThemeProvider defaultTheme="light"`) · Mobil: **cihaz temasını izler** (`'system'`) | `src/components/providers/ThemeProvider.tsx:25`, `apps/mobile/src/lib/theme/theme-storage.ts:30` |
| Şikâyet | Renkler koyu, ferah değil, tutarsız | kullanıcı |

**Şikâyetin teşhisi tek cümlede:** "Koyu" hissi büyük ölçüde **mobil uygulamada cihaz temasını izleyen koyu tema** ile **web'deki koyu tema**dan geliyor; ikisi de neredeyse siyah lacivert bir zemin (`#020817`, açıklık %4,9) üzerine zeminden yalnız 1,12:1 kontrastla ayrışan kartlar koyuyor. "Tutarsız" hissi ise web'de: renklerin yarıdan fazlası tasarım token'ından değil ham Tailwind paletinden (22 renk ailesi) geliyor.

**Kanıt kaynakları:** (1) kod ölçümü — betikler, sayılar bu belgede; (2) canlı web (apartora.com yönetici panosu, açık + koyu tema, 2026-09-19); (3) mobil yönetici uygulamasının 72 ekranlık açık/koyu kare çiftleri (`apps/mobile/docs/ux-denetim/kareler/cift/`, git dışı); (4) dört rol için kod üzerinden görev yolu çıkarımı. Figma yok.

---

## 1. Mevcut durum envanteri

### 1.1 Renkler

**Token paleti (ölçülen hex karşılıkları).** Web ve mobil aynı shadcn token adlarını kullanıyor ama değerler birebir aynı değil.

| Token | Web açık | Mobil açık | Web koyu | Mobil koyu | Not |
|---|---|---|---|---|---|
| `--background` | `#F8FAFC` | `#F8FAFC` | **`#020817`** | **`#020817`** | Koyu zemin açıklığı %4,9 — neredeyse siyah |
| `--card` | `#FFFFFF` | `#FFFFFF` | `#0F1729` | `#0F1729` | Koyu: zeminle kontrast **1,12:1** |
| `--muted` / `--secondary` / `--accent` | `#F1F5F9` (96,1%) | `#E6EDF4` (93%) | `#1E293B` | `#1E293B` | Açık temada iki hedef farklı |
| `--muted-foreground` | `#64748B` | `#64748B` | `#94A3B8` | `#94A3B8` | |
| `--border` | `#E2E8F0` (91,4%) | `#D7DFEA` (88%) | `#1E293B` | `#1E293B` | Web açık: zeminle 1,18:1 |
| `--input` | = border | = border | = border | `#364154` (27%) | Mobil koyuda ayrıldı, web'de ayrılmadı |
| `--primary` | `#2563EB` | `#2563EB` | `#3B82F6` | `#3B82F6` | Koyu temada buton yazısı **lacivert** (`#0F172A`) |
| `--destructive` | `#EF4444` (red-500) | `#DC2626` (red-600) | **`#7F1D1D`** | `#EF4444` | Mobil bugün düzeltildi, **web hâlâ bordo** |
| `--success` | `#16A34A` | `#0D9B6C` | `#22C55E` | `#25A777` | İki hedef farklı yeşil |
| `--warning` | `#F59E0B` | `#DB7706` | `#F59E0B` | `#F59F0A` | |
| `--radius` | 0,5rem (8px) | **0,625rem (10px)** | | | Aynı `rounded-lg` iki hedefte farklı |

Mobilde bunlara ek olarak native bileşenler için elle tutulan ikinci bir JS paleti var (`apps/mobile/src/constants/theme.ts:12-45`). Orada koyu `backgroundElement: '#1E293B'` yorumda "card 222.2 47% 11%" diye anılıyor; oysa kart `#0F1729`, `#1E293B` `--muted` tonudur (`theme.ts:37`). Yani native yüzeyler (klavye araç çubuğu vb.) NativeWind kartından bir ton açık çiziliyor.

**Kodda kullanım (ölçüm):**

| Ölçü | Web (5.093 dosya) | Mobil (1.753 dosya) |
|---|---:|---:|
| Semantik token sınıfı (`bg-card`, `text-muted-foreground`…) | 19.119 | 5.113 |
| **Ham palet sınıfı** (`text-green-600`, `bg-blue-50`…, `dark:` öneksiz) | **20.402** | 613 |
| Ham palet sınıfının `dark:` karşılıkları | 10.824 | 253 |
| Kullanılan renk ailesi | **22** (green 3.437 · blue 3.217 · red 3.098 · amber 2.328 · orange 1.582 · purple 1.097 · emerald 1.039 · yellow 861 · indigo 496 · teal 483 · violet 328 · gray 264 · slate 237 · cyan 182 · rose 160 · sky 118 · pink 84 · …) | 18 (amber 240 · emerald 174 · …) |
| Hex literal (UI katmanı) | 1.054 kullanım · **185 farklı değer** · 127 dosya (PDF/e-posta/dışa aktarım hariç; onlarla toplam 2.612 / 274) | 259 kullanım · 97 farklı değer |
| Ham palet kullanıp **hiç `dark:` karşılığı olmayan** dosya | **286 / 1.170** | — |
| `bg-gradient-to-*` | **970** (yönetici/sakin/güvenlik ekranlarında 192) | 13 `LinearGradient` |
| Semantik durum token'ı (`success`/`warning`/`info`/`error`) | 58 / 47 / 65 / 1 | 70 / 68 / — / — |

Yorum: web'de durum renkleri için token **tanımlı ama kullanılmıyor** (`text-success` 58 kez, buna karşılık `text-green-*` binlerce). Mobil bu konuda disiplinli.

### 1.2 Tipografi

| Ölçü | Web | Mobil |
|---|---|---|
| Aile | Plus Jakarta Sans 400/500/600/700 + DM Sans 500/600/700, ikisi de `preload` | Sistem fontu; `--font-display: Spline Sans, Inter` tanımlı ama yüklenmiyor (`global.css:6`) |
| Başlık fontu kullanımı | `font-heading` yalnız **13** yerde → DM Sans 3 ağırlıkla yükleniyor ama neredeyse hiç görünmüyor | Büyük rakamlarda iOS'ta SF Pro Rounded (`NumericDisplayFont`) |
| Boyut dağılımı | `text-sm` 8.049 · **`text-xs` 7.129** · `text-lg` 1.182 · `text-2xl` 1.177 · `text-base` 706 · `text-xl` 415 · `text-3xl` 303 | `text-sm` 1.408 · `text-xs` 1.327 · `text-base` 425 · `text-lg` 135 · `text-2xl` 98 |
| Ölçek dışı boyut | **`text-[10px]` 302 · `text-[11px]` 184** · `text-[9px]` 4 · `text-[13px]` 9 · `text-[15px]` 6 | `text-[11px]` 84 · `text-[10px]` 34 · `text-[8px]` 1 · `text-[9px]` 1 |
| Tanımlı tipografi token'ı | `text-heading-1…3`, `text-body`, `text-small` → toplam **46** kullanım (token var, benimsenmemiş) | yok |
| Ağırlık | medium 5.078 · bold 1.927 · semibold 1.759 | medium 749 · semibold 608 · bold 288 |

Yorum: `text-xs` (12px) neredeyse `text-sm` kadar kullanılıyor; 12px ve altı toplam **7.625** yer. Bu yoğunluk, "sıkışık, ferah değil" hissinin tipografik kaynağı.

### 1.3 Boşluk

- Izgara 4px tabanlı ve büyük ölçüde tutarlı: `2` (12.455), `4` (7.347), `3` (6.423), `1` (4.440), `6` (2.914).
- Yarım adımlar yaygın: `1.5` 1.621 · `0.5` 1.019 · `2.5` 310 · `3.5` 36 (web). Mobil benzer (`1.5` 277, `2.5` 76).
- Web'de tanımlı duyarlı boşluk token'ları (`p-page`, `p-card`, `p-section`, `gap-element` — `tailwind.config.ts`) → **0–1 kullanım**. Sayfa kenar boşluğu her sayfada elle yazılıyor.
- Kart iç boşluğu tek değil: web kartlarında `p-3`, `p-4`, `p-6` üçü de yaygın; shadcn `CardHeader`/`CardContent` `p-6` iken dashboard kartları `p-3`/`p-4` kullanıyor (ekranda "Hızlı İşlemler" `p-3`, KPI kartları `p-6`).

### 1.4 Köşe yarıçapı (radius)

| Değer | Web | Mobil |
|---|---:|---:|
| `rounded-lg` | 2.326 | 343 |
| `rounded-full` | 1.432 | 309 |
| `rounded` (4px) | 744 | 19 |
| `rounded-md` | 641 | 31 |
| `rounded-xl` | 342 | 79 |
| `rounded-2xl` | 172 | 24 |
| `rounded-sm` | 61 | 7 |
| `rounded-3xl` | 51 | 1 |
| Keyfi (`rounded-[3rem]`, `[2.5rem]`, `[2rem]`, `[0.9rem]`, `[1.8rem]`…) | 29 | 1 |

Web'de 8 adım + keyfi değerler; kartta `rounded-lg`, buton `rounded-md`, dashboard parçalarında `rounded-xl`/`2xl` karışık. Aynı sınıf iki hedefte farklı piksel veriyor (`--radius` 8px ↔ 10px).

### 1.5 Gölge

| Değer | Web | Mobil |
|---|---:|---:|
| `shadow-lg` | **297** | 2 |
| `shadow-sm` | 168 | 1 |
| `shadow-md` | 165 | — |
| `shadow-xl` | 101 | — |
| `shadow-2xl` | 95 | — |
| `shadow` | 43 | 1 |
| Özel token (`soft`, `glass`, `button`, `hover`) | 18 | — |
| Keyfi (`shadow-[0_-4px_20px_rgba(0,0,0,0.3)]` vb.) | ~40, 15+ farklı tarif | — |

Web'de en çok kullanılan gölge en ağırı (`shadow-lg`). Koyu zeminde gölge görünmediği için web koyu temada derinlik tamamen kayboluyor; mobil bunu `surface-2/3` yükselti token'larıyla çözmüş (`apps/mobile/src/global.css`), web'de bu token yok.

### 1.6 Tekrar eden ama farklı tanımlanmış bileşenler

**Kanonik primitiflerin ölçüleri iki hedefte farklı:**

| Bileşen | Web (`src/components/ui/`) | Mobil (`apps/mobile/src/components/ui/`) |
|---|---|---|
| Button | `rounded-md text-sm`; default **40px**, sm **36px**, lg 44, icon 44 · 6 varyant · içerik `children` | `rounded-lg text-base`; default **48**, sm **44**, lg 56 · 5 varyant · içerik `label` prop |
| Card | `rounded-lg border shadow-sm`, header/content `p-6`, başlık `text-2xl` | `rounded-xl border p-4`, gölge `elevation` |
| Input | `h-11 rounded-md` | `h-12 rounded-lg` |
| Badge | `rounded-full text-xs font-semibold`, 8 varyant; `destructive` ile `danger` aynı iş; success/warning/info ham `green-500`/`yellow-500`/`blue-500` | varyant yok, renk `bgClass`/`textClass` ile (189 override / 120 dosya) |
| İstatistik kartı | `enhanced-stats-card` (`rounded-xl p-5`, `text-3xl`) — yalnız 3 dosya kullanıyor | `stat-tile` (`rounded-lg p-3`, `text-lg`, Card kullanmıyor) + `summary-card` (`text-xl`) |

**Kopyalar ve atlanan primitifler (ölçüm):**

| Tür | Web | Mobil |
|---|---|---|
| İstatistik/KPI kartı | **42 tanım** (16'sı birebir `StatCard` adında; 5 `ModernStatsCard`, ikisi byte-byte aynı çift: `manager/events` ≡ `semimanager/events`, `semiadmin/events` ≡ `superadmin/events`) | 8 yerel tanım (3 `StatCard`, 2 `StatTile`, 2 `SummaryCard`) |
| Durum rozeti | **42 bileşen + 44 `getStatusBadge`/`renderStatusBadge` yardımcısı**; kanonik `StatusBadge` 7 dosyada | 15 yerel Badge/Pill + 87 elle yazılmış pill |
| Kart | `ui/` dışında 187 `*Card` tanımı; elle yazılmış kart `<div>` 143 satır / 65 dosya (4 farklı radius) | 150 `*Card` tanımı (146'sı `features/`); elle kart `View` 90 satır (`rounded-lg` 57 / `rounded-xl` 30 — kanonik `rounded-xl`) |
| Boş durum | 3 paralel aile (`ui/empty-state`, `feedback/empty-states`, `lib/table/.../TableEmptyState`); `TableEmptyState` **iki ayrı yerde** tanımlı + 7 yerel | 1 yerel |
| Sayfa başlığı | `PageHeader` 8 dosya; **287 ham `<h1>`**, 5 farklı boyut (`2xl` 120 · `3xl` 96 · `xl` 37 · `4xl` 14 · `lg` 5) | `ScreenHeader` 131 dosya (`text-lg`), ama `features/` içinde 94 elle `text-2xl font-bold` başlık |
| Silme onayı | `ConfirmDeleteDialog` (48) yanında **5 yerel `DeleteConfirmDialog`** + `ui/modal` ConfirmDialog | 4 yerel silme modalı |
| Ham kontrol | **435 ham `<button>`** / 231 dosya · 27 metin `<input>` · 54 ham checkbox · 39 `<select>` | 19 ham `TextInput` · 33 kare ikon-Pressable (16'sı 44pt altı) |
| Buton yüksekliği override | 4.314 `<Button>`un **929'unda** `className` ile yükseklik ezilmiş, **11 farklı değer** (`h-8` 264 · `h-11` 239 · `h-7` 160 · `h-9` 87…); `size="icon"` 357 butonun **163'ü** 40px ve altına küçültülmüş | — |
| Alt gezinme | 5 `*BottomNav`; `CleaningBottomNav` ortak `PortalBottomNav`'ı kullanmıyor | tek `AppTabs` |

### 1.7 Tutarsızlıklar listesi (özet)

1. Aynı token adı iki hedefte farklı değer (`muted`, `border`, `success`, `destructive`, `radius`).
2. Renklerin %52'si ham palet, 22 renk ailesi, 185 farklı hex (UI katmanı).
3. Durum renkleri token yerine ham (`success` 58 kez, `green-*` 3.437 kez).
4. Tipografi token'ları tanımlı ama kullanılmıyor (46 kullanım); 7 keyfi yazı boyutu; başlık fontu 13 yerde.
5. Boşluk token'ları (`p-page`, `p-card`) 0 kullanım.
6. 8 radius adımı + 29 keyfi; 6 gölge adımı + 15'ten fazla keyfi gölge; 970 gradient.
7. "sm" buton web'de 36px, mobilde 44px; web'de 929 elle yükseklik ezmesi.
8. 42 istatistik kartı, 42 durum rozeti, 3 boş durum ailesi, 287 ham `<h1>`, 5 yerel silme onayı.
9. Koyu tema: 286 dosyada hiç `dark:` yok; 2.660 tonlu koyu kutu; web'de yükselti token'ı yok.
10. Web ve mobil başlık/ekran adları farklı (ör. sakin: "Aidatlarım" / "Ödemelerim" / "Borç Durumu" / "Özet" aynı yere).

## 2. Sorunlar

### 2.1 Görsel tutarsızlıklar

1. **Renk anlamı kaybolmuş (web).** Yönetici panosundaki "Hızlı İşlemler" 7 kısayolu 7 farklı renkle boyuyor: mavi, yeşil, zümrüt, mor, turuncu, **kırmızı**, çivit (`src/components/dashboard/manager-dashboard/QuickActionsCompact.tsx:49-56, 74-122`). "Bakım Talepleri" kırmızı çiziliyor; kırmızı başka her yerde "hata/borç/tehlike" demek. Aynı desen mobil web çubuğunda da var (`MobileQuickActionsBar.tsx:77-108`). Renk bilgi taşımıyor, dekorasyon oluyor. Canlı panoda (2026-09-19, açık tema) doğrulandı.
2. **Durum renkleri token'dan değil ham paletten.** "Başarılı" için `green-*` ve `emerald-*` karışık kullanılıyor (3.437 + 1.039), "uyarı" için `amber`, `yellow` ve `orange` (2.328 + 861 + 1.582). Tanımlı `success` / `warning` token'ları web'de 58 ve 47 kez kullanılmış. Sonuç: aynı "gecikmiş" durumu bir ekranda turuncu, diğerinde kehribar.
3. **Web ve mobil aynı değerleri paylaşmıyor.** `--muted` (96,1% ↔ 93%), `--border` (91,4% ↔ 88%), `--success` (yeşil ↔ zümrüt), `--destructive` (red-500 ↔ red-600), `--radius` (8px ↔ 10px) farklı. Ortak token dosyası yok; ikisi elle senkron tutuluyor (`coding-patterns.md §7` bunu açık borç olarak zaten yazıyor).
4. **Tipografi ölçeği yok.** 12 standart + 7 keyfi boyut; kart başlıkları bir kartta `text-base`, yanındakinde `text-2xl` (canlı panoda "Bu Ay Mali Durum" ile "Son 6 Ay Mali Durum" yan yana farklı boyutta). Tanımlı `text-heading-*` token'ları 46 kez kullanılmış.
5. **Başlık fontu boşa yükleniyor.** DM Sans 3 ağırlıkla `preload` ediliyor ama `font-heading` 13 yerde. Ya kullanılmalı ya kaldırılmalı (ilk yüklemede gereksiz font indirimi).
6. **Yüzey ve derinlik dili yok.** Web'de 8 radius adımı + 29 keyfi değer, 6 gölge adımı + 15'ten fazla keyfi gölge tarifi; 970 gradient. Kart kimi yerde gölgeyle, kimi yerde kenarlıkla, kimi yerde gradyanla ayrışıyor.
7. **Pastel "gökkuşağı" kutular.** Uygulama içinde 192 `bg-gradient-to-*`; çoğu `from-green-50`, `from-blue-50`, `from-amber-50`, `from-red-50`, `from-purple-50` pastel kutular. Tek başına her biri hafif ama bir ekranda 4-6 farklı renkli kutu dağınık görünüm veriyor.

### 2.2 Erişilebilirlik

**Kontrast (WCAG AA: normal yazı 4,5:1, büyük yazı ve UI sınırı 3:1).** Oranlar token değerlerinden hesaplandı.

| Çift | Web açık | Mobil açık | Web koyu | Mobil koyu | Durum |
|---|---:|---:|---:|---:|---|
| Ana yazı / zemin | 19,0 | 19,1 | 19,1 | 19,1 | ✅ |
| İkincil yazı (`muted-foreground`) / kart | 4,75 | 4,75 | 6,99 | 6,99 | ✅ sınırda (açık) |
| İkincil yazı / `muted` zemin (tablo başlığı, sekme şeridi) | **4,34** | **4,03** | 5,70 | 5,70 | ❌ açık temada |
| Kırmızı yazı (`text-destructive`) / kart | **3,76** | 4,83 | **1,79** | 4,76 | ❌ web iki temada da |
| Yeşil `success` yazı / kart | **3,29** | **3,55** | 7,87 | 5,88 | ❌ açık temada |
| Uyarı `warning` yazı / kart | **2,15** | **3,15** | 8,34 | 8,38 | ❌ açık temada |
| Kenarlık / zemin (form alanı sınırı) | **1,18** | **1,28** | **1,37** | 1,74 (`--input`) | ❌ hepsi 3:1 altında |
| Kart / zemin (yüzey ayrımı) | 1,05 | 1,05 | 1,12 | 1,12 | ⚠️ ayrım yalnız gölgeyle (web) |
| Birincil buton yazısı / buton | 4,94 | 4,94 | 4,85 | 4,85 | ✅ |

Ham palette açık temada düşük kontrastlı yazı tonları (`dark:` öneksiz, beyaz zeminde oran): `text-red-500` **357** yer (3,76) · `text-green-500` **285** (2,28) · `text-orange-500` 141 (2,80) · `text-amber-500` 125 (2,15) · `text-yellow-500` 73 (1,92) · `text-teal-500` 73 (2,49) · `text-emerald-500` 41 (2,54). Toplam ~1.100 yazı yeri AA'nın belirgin altında. Okunur eşdeğerler: `green-700` / `amber-700` (5,0:1).

Web koyu `--destructive` (`src/styles/globals.css:453` → `0 62.8% 30.6%` = `#7F1D1D`) 314 dosyada / 678 yerde yazı rengi olarak kullanılıyor ve kartta **1,79:1** veriyor. Aynı hata mobilde bugün düzeltildi (`apps/mobile/src/global.css` yorumu, mandal `apps/mobile/src/__tests__/tema-kontrast.guard.test.ts`); **web'e taşınmadı, web'de kontrast mandalı yok.**

**Dokunma alanı.** Web `Button` varsayılanı `h-10` (40px), `sm` `h-9` (36px) — ikisi de 44px altında; yalnız `icon` 44px (`src/components/ui/button.tsx:23-26`). Web'de `h-8` sınıfı 1.051, `h-7` 288 yerde (hepsi etkileşimli değil). Mobilde liste başlığı sekme/çipleri ~32dp (`apps/mobile/docs/ux-denetim/RAPOR.md`, `list-header.tsx`).

**Yazı boyutu.** Web'de 10-11px 486 yer, 12px 7.129 yer; mobilde 10-11px 118 yer. Web yönetici panosunda istatistik kartı başlıkları mobilde 11px (`src/components/dashboard/manager-dashboard/StatCards.tsx:85,162,209`), dekont kartında 10px (`PaymentApprovalCard.tsx:215,220,296`).

**Koyu tema kapsamı.** Ham palet kullanan 1.170 web dosyasının **286'sında hiç `dark:` karşılığı yok** — koyu temada bu ekranlarda açık tema pastel kutuları (`bg-red-50` vb.) parlak adacıklar olarak kalıyor ya da koyu yazı koyu zemine düşüyor. En yoğunları: `superadmin/quotas/QuotaUsageReport.tsx` (58), `superadmin/site-dashboard/SiteDashboardTab.tsx` (51), `app/security/incident-reports/incident-reports-content.tsx` (34), `app/semimanager/security/page.tsx` (28), `manager/dues/settings/MonthlyDuesAmounts.tsx` (25).

### 2.3 UX problemleri

**Sahte başarı (en ağır sınıf).** Düğme hiçbir şey kaydetmeden/göndermeden "başarılı" toast'ı gösteriyor. Kullanıcı işin yapıldığını sanıyor; tip, lint ve testler yeşil kalıyor. Bu denetimde bulunanlar:

| Yer | Ne diyor | Gerçekte | Durum |
|---|---|---|---|
| Güvenlik → Acil Durum Numaraları → "Acil Durum Uyarısı Gönder" (`src/app/security/emergency-contacts/page.tsx`) | "Tüm güvenlik personeline ve site yöneticilerine bildirim iletildi" | Hiçbir şey gönderilmiyordu | ✅ **Düzeltildi** (`bbd0398a0`) |
| Yönetici → Aidat ay detayı → Ödeme düzenle → Kaydet (`src/app/manager/dues/[month]/page.tsx:1302-1306`) | "Ödeme güncellendi" | Hiçbir şey kaydedilmiyor, diyalog kapanıyor | 🔴 Karar bekliyor (§6) |
| Yönetici → Aidat ay detayı → toplu "Rapor oluştur" (`:1050-1056`) | "N kayıt için rapor oluşturuldu" | Rapor üretilmiyor | 🔴 Karar bekliyor |
| Yardımcı yönetici → Kullanıcılar → Düzenle → Kaydet (`src/app/semimanager/users/page.tsx:771-775`) | "Kullanıcı güncellendi" | Hiçbir şey kaydedilmiyor | 🔴 Karar bekliyor |
| Güvenlik → Bildirimler: okundu işaretle / sil / ayar değiştir (`src/components/notifications/SecurityNotificationsContent.tsx:236-276`) | "Silindi", "Ayar güncellendi" | Yalnız ekrandaki liste değişiyor; yenileyince geri geliyor (kod yorumu bunu kabul ediyor) | 🔴 Karar bekliyor |

**Kaybolma / çıkmaz sokak.**
- Web sakin: alt çubuktaki "Talepler" bakım taleplerini listelemiyor; "Yeni Talep" formundaki banner kullanıcıyı başka sayfaya atıp formu sıfırlıyor (`src/app/resident/requests/page.tsx:91, 884-895`).
- Web sakin: panodaki duyuru öğesi duyuruyu açmıyor, genel listeye bırakıyor (`RecentAnnouncements.tsx:48,66,77`).
- Web sakin (mobil tarayıcı): Aidatlarım, Talepler, Duyurular sayfaları alt gezinmeyi kendi 10px'lik sekme çubuğuyla örtüyor; görevler arası geçiş kopuyor (`src/components/ui/responsive-tabs.tsx:341`).
- Web güvenlik: özet "Detaylar" düğmeleri yalnız bilgi toast'ı gösteriyor (`src/app/security/dashboard/page.tsx:618-628, 684-693`).
- Mobil yönetici: 73 ekranın 58'i başka ekrana geçiş vermiyor (`sonuc/yapi.md`).
- Dış görevli: web'de hiç arayüz yok; mobilde daire kartına dokunulamıyor.

**Fazla adım.**
- Web sakin ödeme: kartta "Öde" önce detay diyaloğunu açıyor, sonra yöntem, sonra portal dışı tam sayfa ödeme — 5 adım; mobilde 2-3 (`TenantPaymentOverview.tsx:154-157`, `usePaymentFlow.tsx:122-151`).
- Web yönetici: panodaki "gecikmiş aidat" uyarısı borçlu listesine değil daire seçimiyle başlayan sihirbaza gidiyor.
- Web güvenlik: ziyaretçi "Yeni" butonu formu açmak için ikinci dokunuş istiyor.

**Belirsiz geri bildirim.**
- Web sakin bakım/dekont formlarında doğrulama hataları yalnız toast; alan altında görünmüyor (`PaymentReportDialog.tsx:241,245`, `resident/maintenance/page.tsx:276-278`).
- Web yönetici bakım "İptal" ve güvenlik "Teslim Et" onaysız, geri alınamaz durum değişikliği.
- Web güvenlik acil alarmı onaysız ve tür seçimsiz; mobilde onay + tür seçimi var — aynı rolde iki farklı davranış.

**Terim ve ad karmaşası.**
- Sakin: aynı yere 5 ad ("Aidatlarım", "Ödemelerim", "Ödeme Özeti", "Borç Durumu", "Özet"); "Bildirim" hem ödeme dekontu hem duyuru bildirimi; muhasebe dili ("mahsup edildi", "KMK 42").
- Yönetici: menü "Bakım ve Onarım" / başlık "Bakım Yönetimi" / Gelen Kutusu "Arıza & Bakım"; iki ayrı "Gelen Kutusu".
- "Muhasebeci" iki farklı şeye ad: semimanager şablonu ve external rolü.

### 2.4 Koyu görünüme neden olan somut renkler ve alanlar

| # | Kaynak | Değer | Etki | Kanıt |
|---|---|---|---|---|
| 1 | Mobil varsayılan tema = cihaz | `'system'` | Kullanıcıların çoğu telefonu koyu temada kullanıyor (`CLAUDE.md`), dolayısıyla uygulamayı ilk açışta koyu görüyor | `apps/mobile/src/lib/theme/theme-storage.ts:30` |
| 2 | Koyu zemin | `#020817` (HSL 222 **84%** 4,9%) | Doygunluğu yüksek, açıklığı çok düşük "mürekkep lacivert" — siyaha yakın; Material'ın önerdiği koyu zemin `#121212` (~%7, doygunluksuz); Apple yükseltilmiş yüzeylerde `#1C1C1E` kullanır | `globals.css:435`, `apps/mobile/src/global.css` dark bloğu |
| 3 | Koyu kart | `#0F1729` | Zeminle **1,12:1** → kartlar zemine karışıyor, ekran tek parça koyu blok gibi | `globals.css:439` |
| 4 | Koyu kenarlık = `muted` = `secondary` = `accent` | `#1E293B` | Kenarlık, ikincil buton, sekme zemini, hover hepsi aynı ton → hiyerarşi yok; form kutusu sınırı 1,23:1 | `globals.css:447-455` |
| 5 | Web koyu kenar çubuğu | `--sidebar-background` = zemin | Menü ile içerik arasında panel ayrımı yok (canlı panoda görüldü) | `globals.css` `.dark` sidebar bloğu |
| 6 | Web koyuda gölge | `shadow-lg` 297 yer | Koyu zeminde gölge görünmez; derinlik tamamen kaybolur. Mobilin `surface-2/3` yükselti token'ları web'de yok | §1.5 |
| 7 | Tonlu koyu kutular | `dark:bg-{green,blue,red,amber,orange,yellow}-950/900` **2.660** yer | Koyu temada yeşil/kırmızı/kehribar kutular zeytin, bordo ve kahverengiye dönüyor ("kirli" koyu); canlı panoda "Dikkat Gerektiren İşlemler" satırı kahverengi | `grep dark:bg-*-950` |
| 8 | Web koyu kırmızı | `#7F1D1D` | Borç, gecikme, hata yazıları bordo — okunmuyor ve ekranı daha da karartıyor | `globals.css:453` |
| 9 | Koyu temada birincil buton yazısı | `#0F172A` (lacivert) mavi üstünde | Butonlar "koyu yazılı" görünüyor; açık temada aynı buton beyaz yazılı | `globals.css` `.dark --primary-foreground` |
| 10 | Açık temada "ferah değil" hissi | kart/zemin 1,05 · kenarlık/zemin 1,18 | Beyaz kart neredeyse beyaz zeminde; ayrım yalnız gölgeye kalıyor, yoğun `text-xs` ile birleşince sıkışık | §1.1, §1.2 |

## 3. Kullanıcı rolü analizi

> Dokunuş/tık sayıları koddan çıkarılmış **tahmindir** (yazı yazmak dahil değil). Mobil yönetici görevleri cihazda ayrıca ölçüldü: `apps/mobile/docs/ux-denetim/sonuc/gorevler.json` (15 görevin 8'i doğrudan, 6'sı zorlanarak, 1'i bulunamadı).

### 3.1 Sakin (resident — malik / kiracı)

Kritik görevler koddan doğrulandı: web alt çubuğu Anasayfa · Aidatlarım · Duyurular · Talepler (`src/components/navigation/portal-nav-config.ts:99-102`); mobil Özet · Talepler · Duyurular (`apps/mobile/src/components/tab-config.ts:66-83`).

| Görev | Web | Mobil | Dokunuş (web / mobil) |
|---|---|---|---|
| Borcu görmek ve ödemek | Aidatlarım → kartta "Öde" → detay diyaloğu "Öde" → "Online" → portal dışı PayTR sayfası | Açılış ekranı borç → "Öde" → "Kart" → uygulama içi PayTR | 5 / 3 |
| Arıza talebi açmak ve izlemek | Anasayfa "Bakım Talebi" → "Yeni Talep" → kategori → Gönder; alt çubuktan başlarsa banner başka sayfaya atar (≈7) | Talepler → + → "Bakım/Arıza" → kategori → Gönder | 5 (7) / 5 |
| Duyuruları okumak | Duyurular → 4 istatistik kartı + filtreler geçilir → ikonlu "Detay" (36px, `aria-label` yok) → diyalog | Duyurular → karta dokun, yerinde açılır, okundu yazılır | 2+ / 2 |

Sürtünmeler (kanıtlı):
- Web "Talepler" ile bakım talepleri kopuk (`src/app/resident/requests/page.tsx:91`); web alt çubukta rozet yok, mobilde var.
- Web bakım formunda **fotoğraf eklenemiyor** (`photos: []` sabit, `src/app/resident/maintenance/page.tsx:325`), öncelik yerine tek "Acil" anahtarı; mobilde fotoğraf + 4 seviyeli öncelik.
- Web ödeme portaldan çıkıyor (`window.location.href`, `src/hooks/usePaymentFlow.tsx:122-151`); "Öde" butonu 36px (`UnpaidDuesCard.tsx:482-484`).
- Web "Site Mali Durumu" linki kiracıya da gösteriliyor, sayfa kiracıya 403 veriyor (`resident/my-payments/page.tsx:25-34`, `resident/finance/page.tsx:167-170`); mobil gizliyor.
- Mobilde "Öde" butonları 4 özet kart + grafiğin altında (`payments-overview-screen.tsx:222-284`).
- Web ↔ mobil: sekme sırası, ana ekran (web genel pano / mobil yalnız borç), ödeme düğmeleri (web tek "Öde" / mobil "Öde" + "Bildir"), duyuru filtreleri, talep yapısı (web 3 sayfa / mobil tek liste) farklı.

**En kritik 3 sürtünme (sakin):** (1) web'de bakım talebi "Talepler"den kopuk ve form sıfırlanıyor, (2) web'de ödeme 5 adım + portal dışı, (3) web mobil görünümünde alt gezinme sayfa sekmeleriyle örtülüyor.

### 3.2 Yönetici (manager / semimanager) — web

| Görev | Yol | Tık | Sürtünme |
|---|---|---|---|
| Aidat tahsilatı (borçlu gör → ödeme kaydet → dekont onayı → hatırlat) | Menü "Aidat Yönetimi" → Güncel; pano "Tahsilat Al" → 3 adımlı sihirbaz; Onay Kuyruğu; satır seç → "Hatırlat" diyaloğu | 2-3 · 4-5 · 2 · 4-5 | Panodaki "gecikmiş aidat" uyarısı borçlu listesine değil daire seçimiyle başlayan sihirbaza gidiyor (`src/components/dashboard/**/AttentionRequired.tsx:415-418`). Aynı aidat tablosu iki menüde (`dues/page.tsx:687`, `finance/page.tsx:440`); ödeme kaydı kodu üç yerde (`DuesTable.tsx:405`, `finance/page.tsx:797`, `dues/[month]/page.tsx:406`). Dekont onayı iki sayfada: menüde "Onay Kuyruğu", bildirimlerden menüde olmayan `/manager/finance/payment-approvals` (`src/lib/notifications/payment-report-notice.ts:145`). Dönem detayı 10 sütunlu ham tablo, mobil kart yok (`dues/[month]/page.tsx:448-652`) |
| Arıza/bakım talebini karşılamak | Menü "Bakım ve Onarım" → satır "⋯" → Detay → Başlat/Tamamla/İptal | 1 · 2 · 1 | **Mevcut talep personele atanamıyor**: sayfa "N atanmamış talep" uyarısı gösteriyor (`src/app/manager/maintenance/_components/maintenance-page-content.tsx:628-631`) ama atama eylemi yok; `assigned_to` yalnız yeni talep formunda. Kırmızı "İptal" onay sormadan durumu değiştiriyor (`:979-987`). Ad üç farklı: menü "Bakım ve Onarım", başlık "Bakım Yönetimi", Gelen Kutusu "Arıza & Bakım" |
| Duyuru yayınlamak | Hızlı işlem / menü → "Yeni" → 7 alanlı form → Yayınla → kanal onayı | ~6 | İki ayrı "Gelen Kutusu" (talepler ve duyuru sayfası sekmesi, `announcements/page.tsx:73-74`); "Duyuru" ile "Hızlı Bildirim" farkı kullanıcıya bırakılmış. Olumlu: yayın öncesi kanal onayı var |

Genel: canlı `feature_modules` verisine göre yöneticinin menüsünde 7 grupta **35 modül + koddan 3 = ~38 öğe**; semimanager 20. Pano 17 blok (`src/app/manager/dashboard/page.tsx:288-458`), en üstte 3 banner/nudge kartı üst üste. Mobil yönetici tarafı ayrıca ölçüldü: menü **73 satır**, 73 ekranın 58'i başka ekrana geçiş vermiyor (`apps/mobile/docs/ux-denetim/sonuc/yapi.md`).

**Muhasebe (semimanager "Muhasebeci" şablonu, 26 izin):** 14 menü öğesi görüyor. `permission_key`'i boş modüller yalnız role bakılarak gösterildiği için Personel, Temizlik, QR Noktaları ve Nöbet Defteri muhasebeciye de çıkıyor (`src/lib/sidebar/feature-access.ts:66-67`). Tersine, `approvals.view` olmadığından **Onay Kuyruğu gizli**, oysa `dues.payments` izni var — muhasebeci dekont onayına yalnız bildirimden ulaşabiliyor. Semimanager aidat sayfası yönetici sayfasının 671 satırlık ayrı kopyası.

**En kritik 3 görev ve sürtünmeleri (yönetici):**
1. Tahsilat → aynı iş 2-3 sayfada, uyarı yanlış yere götürüyor.
2. Bakım → talep atanamıyor (web'de görev tamamlanamıyor).
3. İletişim → iki "Gelen Kutusu", Duyuru/Hızlı Bildirim ayrımı belirsiz.

### 3.3 Güvenlik görevlisi (security)

| Görev | Web | Mobil | Dokunuş (web / mobil) |
|---|---|---|---|
| Ziyaretçi kaydı / onayı | ☰ → Ziyaretçi Girişi → Yeni → Kaydet | Ziyaretçi sekmesi → FAB → Kaydet | ~4 / 3 |
| Kargo kabul / teslim | ☰ → Kargo → Yeni → Kaydet; teslim tek dokunuş | Menü → Kargo → FAB → Kaydet | ~4 / 4 |
| Acil durum / devriye | Acil 1 dokunuş; devriye ☰ → Devriye → Başlat → "Kamerayı aç" | Acil: buton → tür seç → Gönder; devriye sekmesi → Başlat → QR | 1 / 2 |

Sürtünmeler:
- **Web'de iki acil düğmesi iki farklı iş yapıyordu.** Özet sayfasındaki düğme tek dokunuşla, onaysız ve tür seçimsiz site geneli bildirim yazıyor (`src/app/security/dashboard/page.tsx:291-304, 424-431`; ağ hatasını hook toast'la gösteriyor). "Acil Durum Numaraları" sayfasındaki aynı etiketli düğme ise **hiçbir şey göndermeden** "Tüm güvenlik personeline ve site yöneticilerine bildirim iletildi" diyordu — ✅ bu oturumda düzeltildi (`bbd0398a0`, artık özetle aynı yoldan gönderiyor). Mobilde aynı iş onay + tür seçimi + uyarı metniyle sunucu ucundan (`/api/security/alert/[type]`) yapılıyor (`apps/mobile/src/features/site-security/emergency-alert-button.tsx:81-130`). Web'i bu akışa hizalamak **karar bekliyor** (§6).
- Web özetindeki "Detaylar" butonları yalnız toast gösteriyor — çıkmaz sokak (`dashboard/page.tsx:618-628, 684-693`).
- Web alt çubukta Devriye ve Kargo yok (`src/components/navigation/portal-nav-config.ts:124-129`); ziyaretçi "Yeni" butonu formu açmak için ikinci dokunuş istiyor (`visitors/page.tsx:204-205`).
- 44px altı hedefler: onayla/reddet `size="sm"` (36px) + `text-xs` (`dashboard/page.tsx:552-574`); yazısız ✓/✗ butonlarında `aria-label` yok (`visitors/page.tsx:296-311`); mobil "Kodu elle gir" ~36pt (`camera-scan-view.tsx:190-195`).
- Kargo teslimi onaysız ve geri alınamaz (web `delivery-tracking-content.tsx:193-201`, mobil `delivery-screen.tsx:83-91`).
- Ev sahibi/daire serbest metin, seçici yok (`visitor-check-in-form.tsx:122-137`, `visitor-entry-content.tsx:419-438`).
- Çevrimdışı kuyruk yalnız devriyede; ziyaretçi ve kargo kuyruksuz (`use-patrol.ts:84`, `use-delivery.ts:3`).
- Tek el/eldiven: "QR Tara" ile kırmızı "Devriyeyi Bitir" yan yana (`patrol-scan-screen.tsx:337-345`), kamerada fener yok.

### 3.4 Temizlik / görevli (cleaning / staff)

| Görev | Web | Mobil | Dokunuş |
|---|---|---|---|
| Günlük görev listesi | Özet → "Görevlerim" | Görevler sekmesi | 1 / 1 |
| QR okut → başlat → tamamla | Özet → QR Okut → Kamerayı aç → Başlat → liste → Tamamla | Görevler → satır → Başlat → liste → Tamamla | ~5+n / ~4+n |
| Fotoğraf kanıtı | **yok** | öncesi/sonrası foto | – / +2 |

Sürtünmeler:
- Web'de fotoğraf kanıtı yok (`src/app/cleaning/scan/page.tsx:294-370`; mobil dosya bunu kendisi yazıyor: `cleaning-scan-screen.tsx:7-8`).
- Web görev satırı taramaya `?checkpoint=<id>` ile gidiyor (`src/app/cleaning/tasks/page.tsx:169`), tarama sayfası yalnız `site_id` ve `code` okuyor (`scan/page.tsx:86-87`) → görev bilgisi kayboluyor, personel QR'ı yeniden aramak zorunda. (Fiziksel okutma zorunluluğu bilinçli olabilir; o durumda parametre de ekrandaki bağlam da kaldırılmalı — **ürün kararı**.)
- Web "Başlat" adımında vazgeç yok; mobilde var.
- Çevrimdışı: web layout gerekçesini yazıyor (`cleaning/layout.tsx:11,50`) ama taramada çevrimdışı işleme yok; mobil tarama da kuyruksuz (`use-cleaning-scan.ts:10-20`). Bodrum/otopark gibi çekmeyen yerlerde iş kaybolur.
- **staff rolünün mobil portalı yok** — kullanıcı sakin arayüzüne düşüyor (`packages/shared/src/roles.ts:115-137`). Web `/staff` görev kartının tamamı durum düğmesi; tamamlanmış göreve dokunmak onaysız "bekliyor"a çeviriyor (`src/app/staff/tasks/page.tsx:34-41, 143-146`).
- Olumlu: büyük butonlar (web `h-14`, mobil `h-11/h-12`).

### 3.5 Dış görevli (external: muhasebeci / avukat / denetçi)

| Görev | Web | Mobil |
|---|---|---|
| Atandığı daireleri ve borcu görmek | **arayüz yok** | "Dosyalarım" açılış ekranı |
| Hukuki belgeyi açmak/indirmek | yok | yok (yalnız sayı rozeti) |
| Ekstre/rapor indirmek | yok | yok |

- Web'de external portalı yok: rol→sayfa eşlemesinde yok (`src/components/auth/RoleGuardWrapper.tsx:43-52`), giriş `/external/dashboard` üretiyor (`src/lib/auth/login-route.ts:630-632`) ama `src/app/external` yok.
- Mobilde daire kartına dokunulamıyor; sunucu belge/ekstre uçlarını açmış olsa da arayüz kullanmıyor (`external-overview-screen.tsx:137-181`, `src/lib/authorization/external-scope.ts:75-90`).
- Menü sekmesi sakin menüsüne düşüyor (Ödeme geçmişi, Makbuzlar — `apps/mobile/src/app/(tabs)/menu.tsx:35-36`).
- "Muhasebeci" adı iki farklı şeye veriliyor: semimanager şablonu ve external rolü.

## 4. Önceliklendirme

Ölçüt: **etki** (kaç kullanıcı, ne sıklıkta, para/güvenlik riski) × **kaldıraç** (tek düzeltme kaç ekranı kurtarır) ÷ **maliyet**.

| Sıra | Ne | Neden önce | Kaldıraç |
|---|---|---|---|
| 1 | **Sahte başarı sınıfı** (4 açık örnek + mandal) | Kullanıcıya yalan söylüyor; biri para ("Ödeme güncellendi"). Görsel iş değil, güven meselesi | Mandal gelecekteki örnekleri de keser |
| 2 | **Koyu tema yüzey paleti** (`--background`, `--card`, `--border`, `--muted`, web'e `surface-2/3`) | Kullanıcıların çoğu mobil + koyu tema; şikâyetin ("koyu, ferah değil") doğrudan kaynağı. Token düzeyinde → **her ekran** tek değişiklikle düzelir | 2 CSS dosyası + mobil JS paleti |
| 3 | **Durum rengi token'ları** (`success`/`warning`/`danger`/`info` → yazı için 700 tonu, rozet için tek bileşen) | Açık temada ~1.100 yazı yeri AA altında; "tutarsız" hissinin ana kaynağı | Web `Badge` + `StatusBadge` + mobil `statusTone` |
| 4 | **Sakin web'i**: ödeme akışı, bakım talebi ↔ Talepler, alt gezinme | En kalabalık rol; en sık iki görev (ödeme, arıza) web'de mobilden 2 kat uzun | 3 sayfa |
| 5 | **Yönetici panosu** (web): 7 renkli hızlı işlem → tek renk, 17 blok → önceliklendirilmiş 8-10 blok, uyarı → doğru hedef | Her gün açılan ilk ekran; ilk izlenim | 1 sayfa |
| 6 | **Buton/Input boyut sözleşmesi** (web `sm` 36→40, default 40→44; 929 elle ezmeyi erit) | Dokunma alanı; mobil web kullanıcıları | `button.tsx` + codemod |
| 7 | **Bileşen birleştirme**: tek `StatCard`, tek `StatusBadge`, tek `EmptyState`, `PageHeader` zorunlu | Uzun vadeli tutarlılık; her yeni ekranın dağınıklığı büyütmesini durdurur | Mandal ile ratchet |
| 8 | Saha personeli (güvenlik/temizlik) çevrimdışı kuyruk, web foto kanıtı, staff mobil portalı, external web portalı | Rol başına kritik ama kullanıcı sayısı düşük; ürün kararı içeriyor | — |

**Neden bu sıra:** 1 güven sorunudur ve kısa sürer. 2 ve 3 token katmanında olduğu için her ekranı aynı anda düzeltir; şikâyetin %80'i buradan gelir. Ekran bazlı işler (4-5) token'lar oturduktan sonra yapılırsa iki kez boyanmaz. Bileşen birleştirme (7) en pahalısı; ratchet mandalıyla zamana yayılmalı.

## 5. Özet tablo

| Alan | Mevcut | Sorun | Öneri yönü |
|---|---|---|---|
| Koyu zemin | `#020817` (HSL 222 84% 4,9%) | Siyaha yakın, çok doygun lacivert; "koyu" şikâyetinin kaynağı | Açıklığı %9'a, doygunluğu %20'ye çek (`#12151C`); zemin → kart → yüzen arası 4'er açıklık puanı (ölçülmüş palet §6 madde 2) |
| Koyu kart / yükselti | Kart `#0F1729`, zeminle 1,12:1; web'de yükselti token'ı yok | Kartlar zemine karışıyor, gölge koyuda görünmüyor | Mobildeki `surface-2/3` tonal yükselti modelini web'e taşı; tek ortak token dosyası (`packages/shared` → iki CSS'e üret) |
| Koyu kenarlık / ikincil yüzey | `border` = `muted` = `accent` = `#1E293B` | Kenarlık, sekme, hover aynı ton; form kutusu sınırı 1,23:1 | Web'de de `--input`'u ayır (mobil 27%), kenarlık ~%22-25 |
| Kırmızı (`destructive`) | Web koyu `#7F1D1D` (1,79:1), açık red-500 (3,76:1) | Borç/gecikme/hata okunmuyordu | ✅ **Düzeltildi** (`20bdccd7b`): mobil ile aynı, mandallı |
| Durum renkleri | `text-green-500` 285 · `amber-500` 125 · `yellow-500` 73 …; token 58/47 kullanım | Açık temada 1,9–3,3:1; aynı durum farklı renk | Yazı için 700 tonu (5:1), tek `StatusBadge`/`statusTone` sözleşmesi, ham `*-500` yazıya mandal |
| Renk sayısı | 22 aile, 185 hex, 970 gradient, 7 renkli hızlı işlem | Renk bilgi taşımıyor, dekor; "gökkuşağı" | Nötr + 1 marka + 4 durum rengi; dekoratif renkli kutular nötr kart + renkli ikon |
| Açık tema ferahlık | kart/zemin 1,05, kenarlık 1,18, `text-xs` 7.129 yer | Düz, sıkışık | Kenarlığı 88%'e (mobil gibi), gövde metni `text-sm`/`text-base`, kart iç boşluğu tek değer (16/24) |
| Tipografi | 12 standart + 7 keyfi boyut; 10-11px 486 yer; DM Sans 13 yerde | Hiyerarşi yok, küçük yazı | 6 adımlı ölçek (12/14/16/18/22/28), 12px altı yasak (mandal), başlık fontunu ya kullan ya kaldır |
| Radius | Web 8 adım + 29 keyfi; web 8px ↔ mobil 10px | Aynı sınıf iki hedefte farklı | 3 adım (8 / 12 / tam), iki hedefte aynı `--radius` |
| Gölge | Web 6 adım + 15+ keyfi; en çok `shadow-lg` | Ağır, tutarsız; koyuda görünmez | 2 gölge (kart, yüzen) + koyuda tonal yükselti |
| Buton | Web default 40px, sm 36px; 929 elle yükseklik; mobil 48/44 | Dokunma alanı altı, iki hedef farklı | Web default 44 / sm 40; ezmeleri erit, ikon buton ≥44 mandalı |
| İstatistik kartı / rozet / boş durum | 42 / 42+44 / 3 aile | Aynı şey 40 kez farklı | Tek bileşen + ratchet mandalı |
| Sayfa başlığı | `PageHeader` 8 dosya, 287 ham `<h1>` | Başlık boyutu sayfadan sayfaya değişiyor | `PageHeader` zorunlu (mandal) |
| Sahte başarı | 5 örnek (1'i düzeltildi) | Kaydetmeden "kaydedildi" | Gerçek uca bağla ya da kontrolü kaldır; toast-only handler mandalı |
| Sakin ödeme (web) | 5 adım, portal dışı | Mobilde 2-3 adım | Karttaki "Öde" doğrudan yöntem seçimine; ödeme diyalog/iframe içinde |
| Sakin talepler (web) | Talepler ↔ bakım kopuk, foto yok | Talep takibi kayboluyor | Tek birleşik talep listesi (mobil gibi), bakım formuna foto |
| Yönetici panosu (web) | 17 blok, 3 banner üst üste | Önemli iş kayboluyor | "Dikkat gerektiren" + 4 KPI + tek renk kısayollar; banner'lar tek şeritte |
| Güvenlik acil alarmı (web) | Onaysız, tür seçimsiz; mobil onaylı + türlü | Aynı rolde iki davranış | Web'i mobil akışına ve `/api/security/alert/[type]` ucuna hizala |
| External rolü | Web'de arayüz yok | Muhasebeci/avukat web'den giremiyor | Salt-okur web portalı ya da girişte yönlendirme |

### Hızlı kazanımlar (5 madde)

1. **Koyu zemin + kart + kenarlık token'larını aç** (web `globals.css` + `globals.public.css` + mobil `global.css` + `constants/theme.ts`, ~12 satır). Tüm uygulamanın koyu görünümü tek commit'te ferahlar. Kontrast mandalı (`tema-kontrast.guard`, iki hedefte hazır) sınırları korur.
2. **Hızlı İşlemler'i tek renge indir** (`QuickActionsCompact.tsx:49-56`, `MobileQuickActionsBar.tsx:77-108`): nötr kart + `text-primary` ikon. En çok görülen ekrandaki "gökkuşağı" kalkar; kırmızı yalnız tehlike demeye başlar.
3. **Durum yazılarında `*-500` → `*-700` / `dark:*-400`** (~1.100 yer, mekanik değişim + mandal). Açık temadaki okunmayan yeşil/kehribar/sarı yazılar AA'yı geçer.
4. **Web `Button` boyutları**: default `h-11` (44px), sm `h-10` (40px) (`button.tsx:23-24`). Tek dosya; mobil web'de dokunma alanı düzelir.
5. **Web sakin alt gezinme** — sayfa sekmelerini alt gezinmenin üstüne taşı (`responsive-tabs.tsx:341` `fixed bottom-0` → üst yapışkan şerit). Sakin görevler arası geçişi geri kazanır.

## 6. Bu oturumda düzeltilenler ve karar bekleyenler

**Düzeltildi (test + commit):**

| Commit | Ne | Test |
|---|---|---|
| `20bdccd7b` | Web `--destructive`: koyu 1,79:1 → 4,76:1, açık 3,76:1 → 4,83:1; mobil ile aynı değer. Yeni web mandalı `tema-kontrast.guard` (pre-push invariant listesinde) | 42/42 (düzeltme öncesi 9 KIRMIZI) |
| `bbd0398a0` | Güvenlik "Acil Durum Uyarısı Gönder" artık gerçekten gönderiyor; sahte başarı toast'ı kaldırıldı, ön koşul hatası görünür, çift gönderim kilidi | 4/4 (düzeltme öncesi 4 KIRMIZI) |

**Karar bekleyenler (her biri için öneri):**

1. **Sahte başarı — kalan 4 örnek.** Her birinde seçim: gerçek uca bağlamak mı, kontrolü kaldırmak mı. *Öneri:* aidat ay detayı "Ödeme düzenle" → aidat tablosunun zaten kullandığı ödeme düzenleme yoluna bağla (para akışı olduğu için önce yaklaşım onayı); "Rapor oluştur" → mevcut dışa aktarmaya bağla ya da kaldır; semimanager "Kullanıcı düzenle" → yöneticinin kullanıcı düzenleme ucuna bağla; güvenlik bildirimleri → `use-notifications` hook'una bağla. Hepsi için "toast-only handler" mandalı.
2. **Koyu zemin tonu** (hızlı kazanım 1). Zemini açmak kart üstündeki renkli yazının kontrastını düşürür; yalnız zemini değiştirmek kırmızıyı kartta 4,41:1'e, maviyi 4,51:1'e indirir (ölçüldü). Bu yüzden öneri **paket hâlinde**:

   | Token | Şimdi | Öneri | Ölçüm |
   |---|---|---|---|
   | `--background` | `#020817` | `222 20% 9%` → `#12151C` | ana yazı 17,4:1 |
   | `--card` | `#0F1729` | `222 18% 13%` → `#1B1F27` | ana yazı 15,8 · ikincil 6,5 |
   | `--surface-2` (yüzen) | web'de yok | `222 16% 16.5%` → `#232731` | ikincil 5,8 |
   | `--border` | `#1E293B` | `220 14% 24%` → `#353A46` | kartta 1,46 (şimdi 1,23) |
   | `--primary` (koyu) | `#3B82F6` | blue-400 `#60A5FA` | kartta 6,5 · yüzende 5,8 |
   | `--destructive` (koyu) | `#EF4444` | red-400 `#F87171` | kartta 6,0 · yüzende 5,4 |
   | dolu buton etiketi | beyaz / lacivert karışık | ikisinde de lacivert `#0F172A` | mavi 7,0 · kırmızı 6,5 |

   Karar gereken nokta: dolu kırmızı butonun etiketi beyazdan laciverte döner (bugün koyu temada mavi buton zaten lacivert etiketli). Önce mobilde bir ekran çifti çekilip karşılaştırılmalı (`wireframe:shots`); iki hedefin kontrast mandalı sınırları korur.
3. **Web güvenlik acil alarmını mobil akışına hizalama** (onay + tür seçimi + `/api/security/alert/[type]`). *Öneri:* evet; iki web düğmesi tek ortak bileşene insin. Not: sunucu ucu test dışı alarmlarda **tüm site kullanıcılarına** (sakinler dahil) bildirim yazıyor — hedef kitle kararı da gerekiyor.
4. **Temizlik görevinden taramaya giden bağlantı** (`?checkpoint=` okunmuyor): fiziksel QR okutma zorunlu mu? Zorunluysa parametre kaldırılsın ve ekranda "noktaya gidip okutun" yazsın; değilse sayfa parametreyi okusun.
5. **External rolü için web**: salt-okur portal mı, "mobil uygulamayı kullanın" yönlendirmesi mi?
6. **Mobil yönetici bilgi mimarisi** (önceki denetimin öneri B'si) hâlâ karar bekliyor (`apps/mobile/docs/ux-denetim/DEVAM_RAPORU.md`).
