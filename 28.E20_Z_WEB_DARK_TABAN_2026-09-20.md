# E20 Z — Web dark tabanı: token ↔ boya ↔ mobil hizası

| Alan | Değer |
|---|---|
| Tarih | 2026-09-20 (~23:10 +03) |
| Web | `https://apartora.com/manager/dashboard` · **Dneme1234** · Yönetici (sahip oturumu) |
| Tema | `apartora-theme: light` → **Koyu Tema** (uygulama menüsü) → ölçüm → **Açık Tema geri alındı** (doğrulandı) |
| Karşılaştırma | Gündem-Y mobil dark (dosya 27 · `E20-Y-mobil-dark-ozet.png`) |
| Renk final | yok · kod yok |

## Kanıt

| Dosya | Ne |
|---|---|
| `E20-Z-web-dark-dashboard.png` | Web dark Ana Sayfa (sidebar + deneme bandı + Dikkat + KPI) |
| `E20-Y-mobil-dark-ozet.png` | Mobil dark Özet (Gündem-Y) |

**Yöntem:** `getComputedStyle` + CSS değişkenleri (hesap) **ve** kareden piksel örnekleme (boya). İkisi **aynı şey değil** — Z1 tam olarak bu farkı ölçüyor.

---

## Z1 — Dark tabanda iki hex var; **görünen** taban mobil ile birebir

| Katman | Hex | Nereden |
|---|---|---|
| `html.dark` arka durağı | **`#030712`** | gömülü `<style>`: `html.dark { background-color:#030712 !important }` |
| `--background` token | **`#12151c`** | `222 20% 9%` → hesap |
| **Kareden boyanan taban** (sidebar + içerik) | **`#12151c`** | piksel örnekleme (sidebar 8245 px · içerik 5588 px) |
| Mobil dark taban (Gündem-Y) | **`#12151c`** | piksel örnekleme |

- **Şimdi:** `getComputedStyle(body).backgroundColor` **`#030712`** döner, ama ekranda görünen taban **`#12151c`**. Sebep: `#030712` yalnız `html` üzerindeki **flaş önleyici arka durak**; uygulama kabuğu (sidebar + içerik sarmalayıcı) token’ı boyayınca üstünü tamamen kapatıyor.
- **Sebep→sonuç:** Gündem-Y’de Claude’un işaret ettiği “`#12151c` bir yerde L0, bir yerde L1” çelişkisi **ölçümle çözüldü**: `#030712` = html arka durağı (kullanıcı görmez), `#12151c` = **gerçek L0** (web + mobil aynı).
- **Tavsiye:** Karar #5 metni “denetim paketi seçilsin mi” yerine **“L0 = `#12151c` hâlihazırda web+mobil ortak; arka durak `#030712` ayrı katman olarak adlandırılsın”** diye güncellensin.
- **Doğrulama:** hesap + piksel **çifte ölçüm** · **doğrulandı**.
- **Yan bulgu (küçük):** Koyu temaya geçtikten sonra `html` üzerinde `style="background-color: rgb(255,255,255)"` **açık tema kalıntısı** duruyor; şu an `!important` kuralı onu bastırıyor → görünür kusur **yok**. Kural değişir/geç yüklenirse beyaz flaş riski; kayıt amaçlı not.

---

## Z2 — Token seti web ↔ mobil: **tam örtüşme**

| Token | Web dark hesabı | Mobil dark ölçümü (Y) | Durum |
|---|---|---|---|
| `--background` | `#12151c` | `#12151c` | **aynı** |
| `--card` | `#1b1f27` | `#1b1f27` | **aynı** |
| `--border` | `#353a46` | `#353a46` | **aynı** |
| `--foreground` | `#f8fafc` | `#f8fafc` | **aynı** |
| `--muted-foreground` | `#94a3b8` | `#94a3b8` | **aynı** |
| `--primary` | `#4a92f7` | `#4a92f7` (CTA/ikon) | **aynı** |

- **Şimdi:** Altı temel katmanın **altısı da** birebir aynı hex.
- **Sebep→sonuç:** Dilim2 boyunca bulunan web↔mobil sapmaları (E13b mor↔mavi, E19p/E19pm, E24r orange↔amber) **taban katmanından gelmiyor**; sapma **bileşen/aksan** seviyesinde. Bu, “iki ürün gibi duruyor” şikâyetinin kökünü daraltıyor: temel paletler ortak.
- **Tavsiye:** Kalan sapmalar bileşen düzeyinde ele alınsın; taban için iş yok.
- **Doğrulama:** CSS değişkeni okuması + mobil piksel karşılaştırması · **doğrulandı**.
- **Sınır:** Web tarafı **hesap**, mobil tarafı **piksel** — mobilde CSS değişkeni yok (RN); eşleşme hex düzeyinde.

---

## Z3 — Deneme bandı dark: **mor yalnız web’de** (E19p ↔ E19pm sapması dark’ta da sürüyor)

| Öğe | Web dark | Mobil dark (Y) |
|---|---|---|
| Yüzey | `#1b1f27` + gradyan `rgba(23,37,84,.2) → rgba(59,7,100,.2)` (blue-950/20 → **purple-950/20**) | `#182232` (düz) |
| Kenar | `#1e40af` (blue-800) | `#27446e` |
| CTA | “Paketleri Gör” bg `#12151c` · fg `#4a92f7` (outline) | “Görüntüle” metin `#4a92f7` |
| Mor | **var** (gradyanın sağ ucu) | **yok** |

- **Şimdi:** Light’ta ölçülen “A taşıyıcı + **B-wash**” kalıbı (Gündem-X) dark’ta **aynen** duruyor; mobil eşleniğinde mor **hiç yok**.
- **Sebep→sonuç:** Aynı bant iki platformda iki farklı aile okutuyor (web: mavi→mor geçiş; mobil: tek mavi) → “aynı ürün” algısını zayıflatan sapma **light’a özgü değil**, tema bağımsız.
- **Tavsiye:** E19p/E19pm satırı “sapma **light+dark**” diye işaretlensin (renk seçimi değil).
- **Doğrulama:** computed gradyan + piksel (`#1f1c32`, `#1c1f31` mor tint) · **doğrulandı**.

---

## Z4 — E19m’e doğrudan kanıt: turuncu **ikon katmanında**, satır A-mavi

| Öğe | Web dark | Mobil dark (Y) |
|---|---|---|
| “Dikkat Gerektiren İşlemler” başlık ikonu | **`#fb923c`** (orange-400) | ikon yok · düz başlık metni |
| Dikkat satırı yüzeyi | **`#172554`** (blue-950) · kenar `#1e40af` | `#1b1f27` (nötr kart) |
| Satır ikonu | A-mavi | `#4a92f7` (A) |
| “Yönet” bağlantısı | `#f8fafc` (nötr) | — |

- **Şimdi:** Web dark’ta turuncu **var ama satırda değil**, bölüm başlığı ikonunda (`#fb923c`). Satır yüzeyi `#172554`/`#1e40af` → dosya 21’in E19 dark kaydıyla **birebir**.
- **Sebep→sonuç:** Gündem-Y’de “mobil Dikkat nötr, turuncu yok” bulgusu ile birleşince E19m yeniden okunuyor: turuncu, **dikkat işaretinin ikon/uyarı katmanı**; taşıyıcı yüzey webde A-mavi, mobilde nötr → asıl sapma **turuncu değil, yüzey**: web bilgi-mavi bant ↔ mobil nötr kart.
- **Tavsiye:** E19m kaydı “mobil turuncu ↔ masaüstü A” yerine **“yüzey sapması: web A-mavi bant ↔ mobil nötr kart; turuncu = başlık ikonu (web `#fb923c`)”** diye yazılsın. Kapatma değil — turuncu bant durumu hâlâ aranıyor.
- **Doğrulama:** piksel (`#fb923c` 5 px çekirdek + kenar yumuşama tonları) + `#172554` 29.449 px · **doğrulandı**.
- **Sınır:** `#431407`/`#fdba74` (dosya 21 mobil turuncu) bu turda **yine üretilemedi**.

---

## Z5 — Yan ölçüm: dark aktif menü öğesi **A solid**

| Bağlam | Ölçüm |
|---|---|
| `/manager/dashboard` · “Ana Sayfa” aktif | **`#4a92f7` solid dolgu** (4024 px) |
| Kayıtlı E18a (`/manager/dues` · “Aidat Yönetimi” aktif) | nötr `#f8fafc` + amber pin — **A aksanı değil** |

- **Şimdi:** Dark’ta aktif menü öğesi **A solid** olabiliyor; E18a’nın “dark aktif = nötr” kaydı **sayfa/örneklem bağlı**.
- **Tavsiye:** E18a satırına kapsam notu: “örneklem `/manager/dues`; `/manager/dashboard`’da aktif öğe **A solid `#4a92f7`**”. Çelişki değil, örneklem genişlemesi.
- **Doğrulama:** piksel · **doğrulandı**.

---

## Oy formatı (Gündem-Z)

```
GUNDEM: Z
Z1_TABAN_IKI_HEX: L0=#12151c-arkadurak=#030712-OK | DUZELT
Z2_TOKEN_ORTUSME: tam-ortusme-OK | DUZELT
Z3_BANT_MOR: light+dark-sapma-OK | DUZELT
Z4_E19M_YENIDEN: yuzey-sapmasi-OK | eski-dil-kalsin
Z5_E18A_KAPSAM: kapsam-notu-OK | sonra-OK
RENK_SECIM: yok-OK
CAPRAZ: (1 cumle)
NOT: (kisa; hex final yok; kod yok)
```
